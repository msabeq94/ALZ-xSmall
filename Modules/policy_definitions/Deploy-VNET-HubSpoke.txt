In Summary:
-This policy automates the deployment of a vNet in a specific region.
-It peers the new vNet to an existing hub network.
-It enforces network consistency and saves time by automating the configuration of these resources without manual intervention.
============================================================================
Need to Provide:

Required Parameters:
1-hubResourceId: Provide the resource ID of the hub vNet (required).
2-vNetCidrRange: Provide the IP range for the new vNet (required).
3-vNetLocation: Provide the Azure region for the new vNet (required).
4-vNetName: Provide a name for the new vNet (required).
5-vNetRgName: Provide the resource group name where the vNet will be deployed (required).

Optional Parameter:
1-dnsServers: Specify DNS servers (optional).
2-vNetPeerUseRemoteGateway: Set true if you want to use the hub’s VPN gateway, false otherwise (optional).



cnceled as well
